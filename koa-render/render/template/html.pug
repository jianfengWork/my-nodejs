doctype
html
  head
    meta(charset="utf-8")
    meta(name="site", content="test")
    title=title
    script.
      window.onload=function (){
        console.log('pug')
      };
  body
    ul
      each user in users
        li(class='user-item clearfix')
          span(class='fl')= user.name
          span(class='fl')= user.password
